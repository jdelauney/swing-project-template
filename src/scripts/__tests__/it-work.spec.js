import { describe, it, expect } from 'vitest';

describe('Vitest test runner', () => {
  it('should work', () => {
    expect(true).toBe(true);
  });

  /* it('should not work', () => {
    expect(true).toBe(false);
  });*/
});
